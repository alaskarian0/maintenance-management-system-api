# Copy to .env and set your PostgreSQL credentials
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/redirect_db"

# Admin password (stored hashed in DB). Used to create the first admin if table is empty.
ADMIN_INITIAL_PASSWORD="Moh@9801"

# CORS — comma-separated origins allowed to call the API
CORS_ORIGIN=http://localhost:4000,http://localhost:4001

# External hierarchy sync endpoint
HIERARCHY_SYNC_URL=http://192.168.0.31:5000/job-information/hierarchy-entities/full

# Residents (سكنة) API
RESIDENTS_API_URL=http://10.10.10.181:22239/export/families
RESIDENTS_API_KEY=8e579c8f04117edd9c7878d9e5ebc817671662b75c0da1f41e7b9fb841e00a83
